export const sendEscalationNotification = (complaintId, adminId) => {
    // Send an email or any notification about the escalation
    console.log(`Complaint ${complaintId} has been escalated to Admin ${adminId}`);
};
